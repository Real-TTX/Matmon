@page "/notifications/senders"
@{
    // Senders are now a tab on the unified /notifications page. Keep this route as a redirect so
    // bookmarks and editor back-links still work (lands on the Senders tab via the #hash).
    Layout = null;
    ViewContext.HttpContext.Response.Redirect("/notifications#senders");
}
