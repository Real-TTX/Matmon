@page "/notifications/receivers"
@{
    // Receivers are now a tab on the unified /notifications page. Keep this route as a redirect so
    // bookmarks and editor back-links still work (lands on the Receivers tab via the #hash).
    Layout = null;
    ViewContext.HttpContext.Response.Redirect("/notifications#receivers");
}
